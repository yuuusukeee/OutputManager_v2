<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.outputmanager.mapper.OutputMapper">

  <select id="selectAll" resultType="com.example.outputmanager.domain.Output">
    SELECT id, user_id, category_id, title, summary, detail, icon, video_url, created_at, updated_at
    FROM outputs
    ORDER BY created_at DESC
  </select>

  <select id="selectById" resultType="com.example.outputmanager.domain.Output">
    SELECT id, user_id, category_id, title, summary, detail, icon, video_url, created_at, updated_at
    FROM outputs
    WHERE id = #{id}
  </select>

  <insert id="insert" parameterType="com.example.outputmanager.domain.Output">
    INSERT INTO outputs (user_id, category_id, title, summary, detail, icon, video_url, created_at, updated_at)
    VALUES (#{userId}, #{categoryId}, #{title}, #{summary}, #{detail}, #{icon}, #{videoUrl}, NOW(), NOW())
  </insert>

  <update id="update" parameterType="com.example.outputmanager.domain.Output">
    UPDATE outputs
    SET title = #{title},
        summary = #{summary},
        detail = #{detail},
        category_id = #{categoryId},
        icon = #{icon},
        video_url = #{videoUrl},
        updated_at = NOW()
    WHERE id = #{id}
  </update>

  <delete id="delete" parameterType="int">
    DELETE FROM outputs WHERE id = #{id}
  </delete>

  <select id="search" resultType="com.example.outputmanager.domain.Output">
    SELECT id, user_id, category_id, title, summary, detail, icon, video_url, created_at, updated_at
    FROM outputs
    WHERE (#{keyword} IS NULL OR title LIKE CONCAT('%', #{keyword}, '%') OR detail LIKE CONCAT('%', #{keyword}, '%') OR summary LIKE CONCAT('%', #{keyword}, '%'))
      AND (#{categoryId} IS NULL OR category_id = #{categoryId})
      AND user_id = #{userId}
    ORDER BY created_at DESC
  </select>

  <select id="countByCategoryId" resultType="int">
    SELECT COUNT(*) FROM outputs WHERE category_id = #{categoryId}
  </select>

  <select id="selectByIds" resultType="com.example.outputmanager.domain.Output">
    SELECT id, user_id, category_id, title, summary, detail, icon, video_url, created_at, updated_at
    FROM outputs
    WHERE id IN
    <foreach collection="ids" item="id" open="(" separator="," close=")">
      #{id}
    </foreach>
    
  </select>
  <select id="selectByUserId" resultType="com.example.outputmanager.domain.Output">
  SELECT id, user_id, category_id, title, summary, detail, icon, video_url, created_at, updated_at
  FROM outputs
  WHERE user_id = #{userId}
  ORDER BY created_at DESC
</select>

</mapper>
